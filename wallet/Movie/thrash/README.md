Panorama files parked for later use.

The Swift source snapshots are stored as `.swift.txt` so they do not compile with the current Movie flow.

The original PNG files are currently in Finder Trash, but this session cannot move files out of `~/.Trash` because macOS denied access:

- `pano_back.png`
- `pano_entrance.png`
- `pano_front.png`

Move those three files from Finder Trash into this folder when needed.
